# serial-capture: host-only (x86_64); GCC = quality gate, Clang = shipped binary.
# termios dependency rules out nolibc.

SRC       := serial-capture.c
BIN       := serial-capture
HOST_ONLY := 1

include ../common.mk
