# perf-event: cross-compiled for all 4 arches.
# nolibc evaluation: SYS_perf_event_open undefined in nolibc sys/syscall.h — keeps musl.

SRC     := perf-event.c
BIN     := perf-event
LOG_TAG := perf

include ../../common.mk
